Edit the existing `RESULT.md` so its complete contents are exactly this sentence followed by one
newline:

Deterministic verification checks the requested artifact instead of trusting a model's completion claim.
